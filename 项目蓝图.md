# 公司项目进度追踪系统蓝图

版本：v0.1（待评审）  
编写日期：2026-09-04  
定位：公司项目进度大屏 + 企业微信自然语言录入 + 目标节点提醒。  
状态：仅完成方案设计，尚未开发、接入企业微信或部署模型。

> 本文中的“已确认”来自当前需求；“建议”是为便于评审提出的默认方案，不代表用户已同意。字段、提醒参数和接入方式经确认后再冻结。施工期间不擅自修改本蓝图；需要调整时先提出变更并取得确认。

## 1. 目标与范围

### 1.1 已确认需求

1. 公司大屏日常展示项目详情、进度和重要节点。
2. 员工在企业微信中通过消息创建项目、汇报进展和进行相关操作。
3. 一个项目可以包含多个大目标，每个目标有时间节点。
4. 系统能识别未及时更新等情况，主动提醒负责人。
5. 员工不必使用固定指令或结构化模板；后台字段格式固定，由小模型提取信息。
6. 用户希望不需要 @机器人。此前的群内免 @偏好尚未撤销，但接入可行性未验证。

### 1.2 第一版建议范围

- 一个公司、一个独立部署实例，不做多租户。
- 项目、目标节点、进展记录、成员权限、提醒记录、大屏六类核心能力。
- 企业微信文本输入；一个小模型负责识别操作与提取字段。
- 最小网页管理入口，用于校正信息、绑定成员、查看历史及处理异常。
- 第一版采用写操作预览确认：解析后显示“将修改什么”，员工确认后保存。
- 查询不需要写入确认；确认卡片或网页确认采用程序规则，不再次交由模型判断。

### 1.3 第一版不做

- 完整 OA、采购财务、审批流平台、工时核算、研发缺陷管理。
- 自动排程、复杂任务依赖、甘特图编辑器。
- 通用 AI 助手、知识库、模型自主调用任意工具或数据库。
- 附件识别、图片识别、语音处理、自动生成日报周报。
- 自动读取所有群聊、未经确认启用会话存档或非官方客户端接管。
- 高可用集群、复杂微服务架构。

## 2. 整体结构与职责

```text
员工企业微信消息
        │
        ▼
消息接入：取得可信发送者、会话标识、消息编号
        │
        ▼
识别身份与访问范围 → 筛选允许查看的项目和目标候选
        │
        ▼
小模型：识别意图、提取字段、指出缺项和歧义
        │
        ▼
后台：格式校验、实体匹配、权限校验、变更预览
        │
        ├── 不完整或不明确 → 追问，不写入
        ├── 查询 → 返回权限范围内的结果
        └── 写操作 → 员工确认 → 再校验 → 事务保存
                                              │
                          ┌───────────────────┴──────────────────┐
                          ▼                                      ▼
                    大屏读取最新数据                       定时检查目标节点
                                                                 │
                                                                 ▼
                                                    生成提醒任务 → 发送 → 记录结果
```

职责边界：

- AI 负责“理解这句话”，不负责“这个人有没有权限”。
- AI 输出是候选数据，不是可直接执行的指令。
- 后台决定项目编号、发送者身份、操作权限、时间戳、状态计算及提醒。
- 大屏只读；提醒程序只读项目并维护提醒记录，不替员工修改进度。
- 模型不可用时，已保存的项目展示与定时提醒仍正常工作。

## 3. 企业微信接入：必须先验证

### 3.1 消息接收与提醒发送分开评估

“员工发消息给系统”和“系统隔几天提醒员工”不是同一个能力。存在消息接收 SDK，不等于可以接收全部群消息；存在主动发送方法，也不等于任意时间都可以给任意员工发送。

已核对的官方 Python SDK 提供长连接、消息事件、模板卡片和主动发送方法，可复用这些通信能力，但企业实际权限、发送对象与时效限制必须实测。[官方 SDK](https://github.com/WecomTeam/wecom-aibot-python-sdk)

此前查到的腾讯云接入说明采用“直接对话”或“群内 @机器人”触发方式，不能据此承诺群内免 @接收。[腾讯云接入说明](https://cloud.tencent.com/document/product/1831/137051)

### 3.2 候选方式与决策

| 方式 | 适用方向 | 蓝图处理 |
|---|---|---|
| 智能机器人单聊 | 自然语言录入、预览确认 | 可作为候选；是否接受从群聊改为单聊须用户确认 |
| 智能机器人群内 @ | 群内交互 | 不满足免 @偏好，不擅自采用 |
| 群内免 @读取消息 | 最接近用户理想体验 | 待验证；没有可用官方权限时不能承诺实现 |
| 自建应用消息 | 定向提醒员工的候选通道 | 核对管理权限、可见范围、身份映射、网络要求和发送限制 |
| 群机器人 Webhook | 群通知的候选通道 | 不能替代员工输入接收通道；需群通知授权，不作为失败时自动兜底 |

不默认购买或开启会话存档。若只有这类能力能满足免 @，先核对支持范围、成本和数据访问要求，再请用户决策。

### 3.3 接入验证通过标准

开发前必须在指定测试范围验证并记录：

1. 实际群类型及公司账号是否支持目标接入方式。
2. 不 @发送一条消息，后台是否收到；单聊和 @场景分别记录，不混淆结果。
3. 后台能否取得可信发送者标识，并绑定到本系统成员。
4. 能否发送确认交互，以及正确识别点击者。
5. 无近期对话的普通员工是否仍能收到提醒，不能只测试创建机器人者或管理员。
6. 长时间间隔后的发送限制；不能只以刚收消息后的回复成功判断提醒可用。
7. 断开连接、重连、重复回调、发送失败时的实际行为。

若免 @无法验证，暂停相关集成实现，请用户明确接受其他入口或继续寻找合规可行方案；不静默改变需求。蓝图和数据模型仍可继续评审。

## 4. 固定后台数据格式（建议稿）

### 4.1 项目

| 字段 | 必填/来源 | 规则 |
|---|---|---|
| 内部 ID、项目编号 | 系统生成 | 稳定唯一，例如 P0001；不由模型分配 |
| 项目名称 | 必填，用户提供 | 1～100 字；重名需区分，不能只靠名称执行修改 |
| 项目说明 | 可选 | 最多 2000 字 |
| 项目负责人 | 必填 | 绑定系统成员；同名人员需选择 |
| 项目成员 | 必填 | 创建时至少包含负责人；创建者按权限规则加入 |
| 计划开始日期 | 必填 | 用户提供，或明确确认建议日期；不能默认为已开始 |
| 原始计划截止日期 | 必填 | 创建后保留，便于比较原计划 |
| 当前计划截止日期 | 必填 | 初始等于原始截止日期；调整需有原因和权限 |
| 状态 | 后台维护 | 未开始、进行中、暂停、已完成、已取消 |
| 总进度 | 后台派生 | 从目标节点计算，不由 AI 独立填写 |
| 大屏可见 | 必填 | 默认关闭，由授权人员确认后开启 |
| 创建者、创建时间、更新时间、版本号 | 系统生成 | 版本号用于防止旧确认覆盖新数据 |

### 4.2 目标节点

每个项目至少包含一个目标，先采用平级节点；不做无限层级任务树。

| 字段 | 必填/来源 | 规则 |
|---|---|---|
| 节点 ID、所属项目、顺序号 | 系统生成/校验 | 节点必须属于指定项目 |
| 目标名称 | 必填 | 1～100 字 |
| 完成标准 | 必填 | 用可核对的结果描述，例如“方案经负责人确认” |
| 负责人 | 必填 | 必须为项目成员；允许与项目负责人不同 |
| 计划开始日期 | 必填 | 决定何时开始检查更新情况 |
| 原始截止日期、当前截止日期 | 必填 | 保留原计划；调整当前日期需要原因 |
| 状态 | 后台校验 | 未开始、进行中、暂停、已完成、已取消 |
| 进度百分比 | 必填 | 0～100；初始 0；无明确数值时不猜测 |
| 最新进展说明 | 可选 | 员工明确汇报的已完成工作 |
| 当前阻碍 | 可选 | 可明确清除；未提及不代表阻碍消失 |
| 下一步安排 | 可选 | 不作为自动执行任务 |
| 预计完成日期 | 可选 | 与计划截止日期独立；预计延期不自动修改计划 |
| 更新间隔 | 必填 | 默认建议 2 个工作日，可由授权人员调整 |
| 最后有效汇报时间 | 系统维护 | 来自已保存的有效进展记录 |
| 实际开始时间、完成时间、暂停/恢复时间 | 系统维护 | 用于状态与提醒计算 |
| 版本号 | 系统维护 | 用于并发修改校验 |

### 4.3 进展记录

采用追加记录，不覆盖历史：记录项目与节点、汇报人、原消息引用、进展说明、进度前后值、阻碍、下一步、预计完成日期、事件发生时间（可选）、系统接收和保存时间。

- 历史补录与当前汇报分开：补录旧进展不重置当前“待更新”计时。
- 更正采用新记录并关联被更正记录；普通成员不能直接擦除历史。
- 修改名称、负责人或截止日期不算有效进展汇报。
- 有明确说明的“无进展，仍在等待供应商”可以算本次有效汇报，但不会清除阻碍或逾期状态。

### 4.4 最小辅助数据

| 数据 | 保存内容 | 用途 |
|---|---|---|
| 成员与项目成员关系 | 企业标识、企微用户标识、内部用户 ID、角色、可访问项目 | 身份绑定和权限 |
| 入站消息 | 通道、企业、消息唯一标识、发送者、接收时间、处理状态、原文受控存储 | 防重复、排错、追溯 |
| 待确认操作 | 来源消息、候选字段、缺项、证据、对象版本、确认者、过期时间 | 预览、追问与确认 |
| 操作审计 | 操作者、对象、变更前后、原因、来源消息、时间 | 追溯和纠错 |
| 提醒任务与发送记录 | 对象、原因、接收人、去重键、状态、尝试次数、平台回执 | 防刷屏、失败处理 |
| 工作日历与提醒设置 | 时区、工作日覆盖、时间窗口、阈值 | 统一时间口径 |

数据保留周期、原文是否脱敏和备份保留时间，在正式上线前由用户确认。

### 4.5 数据一致性校验

- 项目和节点截止日期不得早于各自开始日期。
- 节点计划超出项目当前计划范围时提示冲突，由有权限的人明确调整，不能静默扩大项目期限。
- 负责人必须存在、启用且属于项目；所有模型给出的实体引用均重新校验。
- 状态与进度矛盾时要求确认或更正；已取消节点不能继续接收普通进度更新。
- 一次确认包含的项目、节点和审计变更整体提交或整体失败，不留下部分立项结果。

## 5. 状态与进度口径

### 5.1 进度计算

建议第一版采用所有未取消节点的百分比算术平均值，标明“按节点等权计算”。暂停、未开始节点仍参与计算；已完成节点为 100%。

- 没有有效节点时总进度为空，不能显示已完成。
- 节点增减会改变总进度，需保存变更记录。
- 不引入用户尚未要求的权重配置或工时估算。
- 节点汇报“完成 60%”可提取 60；“大部分好了”只记录文字，不猜百分比。
- 汇报 100% 时提示确认是否达到完成标准，不能仅凭数值自动验收。

### 5.2 状态和风险标签分开

- 状态是工作阶段：未开始、进行中、暂停、已完成、已取消。
- 风险标签可以同时存在：待更新、临期、逾期、有阻碍。
- 超过计划截止时间且未完成，是“计划逾期”，不代表员工事实上没有做工作。
- 项目完成由项目负责人确认；要求所有未取消节点已完成。
- 节点负责人可确认本人节点完成；重新打开已完成节点需项目负责人处理。
- 暂停不自动顺延截止日期；恢复后若已过截止时间仍会出现逾期提示。
- 项目暂停时停止对员工自动催办，大屏保留暂停标识；已完成、已取消项目不再生成提醒。

## 6. AI 解析方案

### 6.1 输入与输出

模型输入仅包含：当前员工消息、服务器给定的时间和时区、该员工可访问的少量项目/节点候选、必要的当前字段、固定字段定义和允许操作列表。

不默认发送完整公司项目库、群聊历史、员工联系方式或密钥。云模型会接触业务文本，服务商和可发送数据范围须用户确认；本地模型是待选部署方式，不在未评测前承诺性能。

第一版允许的意图：创建项目、补充/调整节点、汇报进度、查询项目、申请调整计划日期、标记完成、暂停/恢复、取消。无业务意图返回“忽略”。

输出契约包含：

- 操作意图及契约版本。
- 项目/节点引用候选；后台再次验证归属和访问权限。
- 要设置的字段及值、用户明确要求清空的字段列表。
- 每个提取字段对应的原文片段。
- 缺少字段、歧义、需确认的日期解释。

输出不包含可执行 SQL、代码、API 地址或可信操作者身份。字段外内容拒绝，不把模型输出直接作为数据库更新参数。

厂商的结构化输出能力可辅助保证输出格式，但不能证明内容正确；仍需后台类型、业务和权限校验。[千问结构化输出文档](https://help.aliyun.com/zh/model-studio/qwen-structured-output)

### 6.2 提取与写入规则

1. 没提到的字段不修改；空字符串和空值不能隐式清空现有数据。
2. 缺少必填项只生成草稿，不创建不完整的正式项目。
3. 同名项目、同名员工、指代不明时追问；不得依据模型自报的置信度跳过确认。
4. “预计下周三完成”只填预计日期；只有明确调整计划的意图才提出修改截止日期。
5. “差不多一半”不直接填准确百分比，询问是否按 50% 记录。
6. “下周三”等相对日期以消息时间和 Asia/Shanghai 为基准，预览必须显示绝对日期；歧义时追问。
7. 引用文字、转发内容、举例和假设不自动执行；含多个操作时拆分预览，不静默部分保存。
8. 个人身份、保存时间、权限、提醒计时不得由模型填写。
9. 明确的业务修改与闲聊区分；即便误识别为修改，也必须经过确认和后台校验。
10. 普通成员不能通过“忽略之前规则”“我是管理员”等消息扩大权限；模型没有直接调用数据库或任意工具的能力。

### 6.3 确认、并发与失败

- 每次写入先返回清晰的变更摘要，说明旧值、新值及影响；创建项目展示完整字段与节点。
- 待确认操作绑定发起人、会话和对象版本，建议 30 分钟过期。
- 同时有多个草稿时，不能仅根据一句“确认”猜测目标，使用卡片或明确操作编号。
- 点击确认时再次检查身份、角色、项目归属和最新版本；人员被移出项目后，旧确认不得生效。
- 同一消息通过持久化唯一键防重复，同一确认只能生效一次。
- 事务内保存业务变更、进展记录和审计，事务成功后才告知保存成功。
- 模型超时或格式错误可有限重试，仍失败则告知未保存，并提供网页录入入口；不降级成猜测写入。
- 追问与确认状态存入数据库，不能只留在进程内存；程序重启后不重复创建项目。

### 6.4 模型选择与评测

不在蓝图中锁定型号和版本，先完成样例集，再选择满足质量要求的较小模型。若采用 Python 的 LangChain 封装，遵循现有约定，优先核对并使用 langchain_community 的 DeepSeek/千问原生封装，不用 ChatOpenAI 代理；具体类、版本和结构化输出兼容性在实施时核验。

建议至少准备 60 条脱敏测试消息，覆盖立项、进展、查询、缺项、重名、相对日期、否定、历史补录、闲聊、权限诱导和多操作。

评估：必填字段提取、意图识别、日期解释、越权拦截、误写入、延迟和调用成本。不把“输出了合法 JSON”当作通过。测试集上的越权写入和未确认写入必须为零；这不是对所有真实输入的零风险保证。

## 7. 提醒规则（建议默认值，待确认）

### 7.1 时间口径

- 公司时区：Asia/Shanghai；存储时间戳统一，展示和规则使用公司时区。
- 计划截止日期未写时刻时，预览提示建议按当天 18:00 截止，由用户确认。
- “工作日”由公司日历决定。初始可设周一至周五，但必须可配置节假日与调休，不能声称已自动覆盖法定日历。
- 建议发送窗口为工作日 09:00～18:00。
- 后台建议每 10 分钟检查一次，窗口外只维护待发送任务。

### 7.2 触发条件

| 类型 | 建议规则 | 清除或停止条件 |
|---|---|---|
| 临期 | 当前截止日期前 1 个工作日，尚未完成/取消且未暂停 | 完成、取消、暂停或计划被授权调整；新旧提醒重新计算 |
| 待更新 | 已到计划开始日且节点未完成/取消/暂停，超过约定汇报间隔 | 有效的当前汇报保存后重新计时 |
| 逾期 | 当前时间超过计划截止时刻，节点仍未完成/取消且未暂停 | 完成、取消、暂停，或经授权调整当前计划 |

待更新计时以最后有效当前汇报、实际开始或计划开始、最近恢复时间中适用的较晚时间为起点；第一次没有汇报时也能计算。每 N 个工作日到期的具体时刻以该起点在公司日历上顺延 N 个工作日确定。

- 未开始但已到计划开始日的节点也检查更新，避免始终停在“未开始”而漏催。
- 历史补录不重置计时；同一段汇报的重复回调不重置计时。
- 一条消息只更新明确涉及的节点，不能刷新整个项目所有节点。
- 更新进度可以消除待更新，但不会自动消除逾期或阻碍。

### 7.3 发送与防刷屏

- 默认只提醒节点负责人；缺失、离职或未绑定人员的提醒进入管理员异常列表，不自动扩大收件范围。
- 项目负责人抄送、群内提醒、升级催办暂不默认开启，另行确认。
- 建议同一节点每天最多出现在一条成功提醒中，多种原因合并显示；同一员工多个节点合并成摘要。
- 同时发生时优先显示逾期，再显示待更新和临期，不丢失其他原因。
- 每次真正发送前重新读取状态、版本和负责人；已完成、已暂停、已更新或已换负责人的旧任务取消或重建。
- 持久化任务去重键和发送结果，避免调度重启后刷屏；同一时刻只有一个调度执行者。
- 明确发送失败采用有限退避重试；超时且结果不确定时单独标记，不声称送达，也不承诺平台不支持幂等时绝对不会重复。
- 平台返回成功只记为“平台已接受”，不等同员工已读。
- 发送超过平台限制、收件人失效或凭证错误时记录异常；不能自动改发到其他群或其他人员。
- 服务停机后恢复，只汇总当前仍有效的问题，不补发停机期间每一次历史提醒。

### 7.4 提醒样例

```text
【项目进度待更新】
项目：P0001 展厅改造
目标：采购到位
负责人：李四
当前进度：60%
最近有效更新：2026-09-14 10:00
计划截止：2026-09-20 18:00
原因：已超过约定更新间隔

请说明最新进展、是否有阻碍，以及下一步安排。
可进入项目助手汇报，或打开有权限的项目详情页。
```

消息应带项目/节点的确定引用。入口形式以企微验证结果为准，不假设群内普通回复一定会被系统接收。

## 8. 公司大屏

### 8.1 页面内容

| 区域 | 展示内容 |
|---|---|
| 顶部概览 | 大屏可见项目数、进行中、已完成、含逾期节点项目数、当前时间 |
| 主列表 | 项目名称、负责人、总体进度、下一到期目标、当前计划截止日期、风险标签 |
| 详情区 | 当前轮播项目的全部目标、负责人、各节点进度、最新进展、阻碍和下一步 |
| 底部状态 | 数据更新时间、连接状态、当前页码 |

### 8.2 展示规则

- 优先显示逾期、待更新和临期项目；排序规则固定、可解释。
- 不把所有未完成节点都挤在一屏；详情区建议每 20 秒轮播，长列表分页。
- 建议每 15 秒读取一次最新快照，第一版不要求复杂实时推送。
- 项目大屏可见后，其详情可能被现场人员看到；公开范围必须由公司确认。
- 大屏接口只返回允许展示的字段，不返回原消息、联系方式、模型输出或管理凭证。
- 采用独立只读展示身份，可撤销；不能在前端使用管理员令牌。
- 页面保留最后成功的数据，但连续 60 秒未刷新成功时明显显示“连接异常/数据可能过期”。
- 使用高对比度、足够大的字体、简洁表格和进度条；不优先做装饰性地图和复杂动画。
- 用真实大屏分辨率验证，不以电脑浏览器截图代替现场验收。

## 9. 成员权限与管理入口

### 9.1 建议角色

| 角色 | 能做什么 |
|---|---|
| 普通成员 | 查询所在项目；创建由本人负责的项目草稿并确认；汇报本人负责节点 |
| 项目负责人 | 管理所负责项目的成员、节点、计划日期、公开展示与状态；查看该项目历史 |
| 系统管理员 | 绑定成员、调整全局提醒参数、管理项目权限、处理异常、管理展示账号 |
| 大屏账号 | 仅读取批准展示的数据，无写入能力 |

普通成员创建项目时，不得通过一句话给其他员工分配责任；涉及新增成员或更换负责人的操作需有项目管理权限。角色最终范围由用户评审。

### 9.2 最小网页管理页

1. 项目列表与详情，支持经授权修改和确认。
2. 节点与进展记录，显示原计划、当前计划和预计完成日期。
3. 人员绑定、项目成员和大屏可见设置。
4. 提醒参数、公司工作日历、发送异常。
5. 操作历史、待确认操作及取消草稿。

管理入口的登录方式根据公司现有条件确认；大屏登录与管理登录分离。

## 10. 技术与复用方案（建议，不是已安装清单）

| 层 | 建议 | 边界 |
|---|---|---|
| 网页前端 | Vue 3 + TypeScript + Element Plus | 复用表格、进度条等基础组件；布局自行实现 |
| 大屏适配 | 响应式布局，必要时引入 v-scale-screen | 不为一个页面部署整套低代码大屏平台 |
| 后台 | Python + FastAPI + 明确的字段校验模型 | 一个业务服务，统一处理权限和写入 |
| 数据存储 | 单机试点可用 SQLite | 一个应用实例、持久化目录和备份；正式容量不明，不承诺大规模并发 |
| 调度 | 后台独立调度任务 + 数据库提醒记录 | 与模型独立；必须保证单一执行者，禁止每个 Web 工作进程各自重复启动 |
| 企微接入 | 优先评估官方 Python 智能机器人 SDK | 根据第 3 节结果决定入口和通知通道，不同时盲目维护多套实现 |
| AI | 一个可替换的小模型解析接口 | 型号、托管方式、版本、预算待评测与确认 |

### 10.1 已查到的复用入口

- [企业微信 Python SDK](https://github.com/WecomTeam/wecom-aibot-python-sdk)：消息收发、连接管理和卡片事件；通信层复用，不视为完整业务系统。
- [企业微信 Node.js SDK](https://github.com/WecomTeam/aibot-node-sdk)：另一语言版本，可参考交互示例；Python 验证满足需求时不额外引入 Node 服务。
- [Element Plus](https://github.com/element-plus/element-plus)：大屏和管理页的基础 UI；[表格文档](https://element-plus.org/zh-CN/component/table.html)、[进度条文档](https://element-plus.org/zh-CN/component/progress.html)。
- [v-scale-screen](https://github.com/Alfred-Skyblue/v-scale-screen)：Vue 大屏缩放适配，可选而非强制依赖。
- [DooTask 工具库](https://github.com/dootask/tools/blob/main/server/go/cmd/doo/README.md)：项目、任务操作可作为整套后台复用的备选，但要求运行 DooTask；本版默认不采用，以避免维护超出当前范围的功能。

实施前核对目标版本、许可证和已知问题；本次文档核对不等同代码安全审计。新增依赖先列安装命令，由用户执行，不自动安装。

### 10.2 部署和故障原则

- 首版按一台持续运行的服务器设计，部署位置、系统和网络条件待确认；不把员工电脑睡眠时仍工作作为承诺。
- 模型密钥、企微凭证只放服务端配置，不写在仓库、大屏或日志中。
- 入站消息和待确认操作持久化；耗时解析不阻塞连接心跳或平台要求的回调应答。
- 业务存储和提醒状态共用数据库事务边界；发送外部消息在事务之外执行并记录结果。
- 上线前配置受控访问、HTTPS（涉及网页或回调时）、数据库备份，并完成一次恢复演练。
- 模型故障不影响提醒；企微故障不影响已保存数据的大屏显示；数据刷新失败显示过期提示。
- 验证程序重启后不会丢失已确认写入，也不会重复处理入站消息。

## 11. 实施阶段与验收

各阶段开始前说明变更范围，取得用户确认后再改代码。阶段编号表示顺序，不承诺未评估的人日。

| 阶段 | 工作 | 通过标准 |
|---|---|---|
| P0 接入与需求验证 | 验证免 @消息、人员身份、确认方式、长期主动提醒；冻结字段和权限 | 形成实测结论；入口不符时取得用户的新选择，不带着未解决的承诺继续 |
| P1 数据与最小管理页 | 项目、节点、进展、权限、审计；先不接 AI | 人工录入可完整管理项目；权限、历史、状态和进度计算测试通过 |
| P2 AI 解析与确认 | 样例集、解析契约、追问、草稿、确认、模型失败处理 | 指定评测集通过；未确认不写入；未知字段、越权、重复和并发冲突被拦截 |
| P3 企微闭环 | 接入真实消息、身份映射、确认交互、保存结果回复 | 普通员工能从企微完成一次立项、节点更新和查询；重复回调不重复写入 |
| P4 节点提醒 | 公司日历、临期/待更新/逾期、去重、失败处理 | 模拟时钟测试通过；真实普通员工收到授权范围内提醒；送达异常可定位 |
| P5 大屏与试运行 | 大屏轮播、只读权限、异常提示、部署备份 | 真实屏幕可读；保存后目标 30 秒内显示；断网不伪装成实时数据；恢复演练通过 |

### 11.1 必测场景

1. 自然语言创建带三个目标的项目；缺少负责人或日期时只追问。
2. 两个同名项目，模型不能擅自选一个写入。
3. 非项目成员查询或修改被拒绝，模型输入中不出现无权限项目详情。
4. “采购到六成，灯具下周三到”只更新对应节点，不改计划截止日期。
5. 未提到的阻碍保持原值；明确说“阻碍已解决”才提出清除。
6. 输入“快好了”不编造百分比；输入“假设完成了”不标记完成。
7. 一条回调重复到达、同一确认重复点击，只产生一次业务变更。
8. 预览后他人修改节点，旧确认提示冲突；权限被撤销后不能确认。
9. 节点 A 更新不清除节点 B 的待更新；历史补录不清除当前待更新。
10. 已到开始日期但从未更新的节点能被提醒；未到开始日期不催更新。
11. 节假日、调休、暂停/恢复、截止当日边界按公司日历计算。
12. 已完成后旧提醒不发送；换负责人后不发给原负责人；模型故障时提醒照常。
13. 发送超时有“不确定”记录，不被当作成功；重启不会一次补发大量历史消息。
14. 大屏账号不能访问原消息、密钥、管理接口或未批准展示项目。
15. 模型服务断开、企微连接断开、网页断网分别有明确提示，数据库已有内容可恢复。

## 12. 评审前需要确定的事项

| 优先级 | 问题 | 当前建议/状态 |
|---|---|---|
| 必须先定 | 群内免 @是否不可妥协；若官方接入不可行，是否接受私聊助手 | 未确认，不替用户放弃 |
| 必须先定 | 项目与节点的最终字段、完成标准、哪些字段允许员工修改 | 本文第 4 节为建议稿 |
| 必须先定 | 谁能立项、分派目标、改截止日期、确认完成 | 本文第 9 节为建议稿 |
| 必须先定 | 提醒发给个人还是群；是否抄送负责人 | 建议先个人定向，需验证并确认 |
| 必须先定 | 云模型还是本地模型；哪些业务内容可以发送给模型服务商 | 未确认 |
| 上线前 | 员工人数、项目数、同时显示数量、大屏分辨率 | 未知，影响布局和容量 |
| 上线前 | 工作日历、截止时刻、提醒阈值和发送时段 | 本文第 7 节为建议默认值 |
| 上线前 | 服务部署位置、管理入口登录方式、网络权限和预算 | 未确认 |
| 上线前 | 大屏公开范围、原消息保留期限、备份责任人 | 未确认 |

## 13. 文档交付边界

本次仅新增蓝图文件，不安装软件、不申请企微权限、不调用模型处理公司数据、不发送通知、不创建定时任务、不修改业务代码。

本文可先用于确认需求与评审。冻结后的实施应以已确认字段和规则为准，不因实现方便擅自改动用户需求。
